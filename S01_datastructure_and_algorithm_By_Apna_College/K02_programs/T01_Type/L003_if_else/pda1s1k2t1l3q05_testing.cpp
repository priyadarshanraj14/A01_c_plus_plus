//(L3)if else // (Q5) if 'a' equal 1 or more than one than'if' 
//statement is true and if a = 0 than 'if' statement is false?
#include<iostream>
using namespace std;
int main ()
{
    int a;
    cin>>a;
    if(a) // a=1 than program enter in if statement and a=0 than it consider as false.(1=true,0=false)
    {
        cout<<"hellow";
    }
    else
    {
        cout<<"false";
    }
    return 0;
}