// (L3) if else 
// (CP5) Test (me) :- if 'a' equal 1 or more than one than'if' statement 
//                    is true and if a = 0 than 'if' statement is false
// code :-

#include<iostream>
using namespace std;
int main ()
{
    int a;
    cin>>a;
    if(a)                  
    {
        cout<<"hellow";
    }
    else
    {
        cout<<"false";
    }
    return 0;
}

// a=1 or more than 1, program enter in "if" statement and 
// a=0 than it consider as false.(1=true,0=false)
// also a equal to less than 0 is consider as true